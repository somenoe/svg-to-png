name: Release

on:
  push:
    branches: [main, action-test]
  pull_request:
    branches: [main, action-test]
  release:
    types: [created]

env:
  CARGO_TERM_COLOR: always

jobs:
  build-and-test:
    name: Build and Test
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        rust: [stable]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: ${{ matrix.rust }}
          components: rustfmt, clippy

      - name: Cache cargo registry
        uses: actions/cache@v4
        with:
          path: ~/.cargo/registry
          key: ${{ runner.os }}-cargo-registry-${{ hashFiles('**/Cargo.lock') }}

      - name: Cache cargo index
        uses: actions/cache@v4
        with:
          path: ~/.cargo/git
          key: ${{ runner.os }}-cargo-index-${{ hashFiles('**/Cargo.lock') }}

      - name: Cache cargo build
        uses: actions/cache@v4
        with:
          path: target
          key: ${{ runner.os }}-cargo-build-target-${{ hashFiles('**/Cargo.lock') }}

      - name: Check code formatting
        run: cargo fmt --all --check

      - name: Run Clippy
        run: cargo clippy --all-targets --all-features -- -D warnings

      - name: Build debug
        run: cargo build --verbose

      - name: Run tests
        run: cargo test --verbose

      - name: Build release
        run: cargo build --release --verbose

      - name: Upload release artifacts
        uses: actions/upload-artifact@v4
        with:
          name: svg-to-png-${{ matrix.os }}
          path: |
            target/release/svg-to-png${{ matrix.os == 'windows-latest' && '.exe' || '' }}
            target/release/svgwatcher${{ matrix.os == 'windows-latest' && '.exe' || '' }}
          retention-days: 7

  release:
    name: Create Release
    needs: build-and-test
    if: github.event_name == 'release'
    runs-on: ubuntu-latest

    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Package artifacts as zip
        shell: bash
        run: |
          cd artifacts
          for dir in svg-to-png-*/; do
            os=$(basename "$dir" | sed 's/svg-to-png-//')
            zip -r "svg-to-png-${os}.zip" "$dir"
          done

      - name: Upload release assets
        uses: svenstaro/upload-release-action@v2
        with:
          repo_token: ${{ secrets.GH_PAT }}
          file: artifacts/**/*
          tag: ${{ github.ref_name }}
          overwrite: true
          file_glob: true
